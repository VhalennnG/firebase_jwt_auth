// const request = require("supertest");
// const app = require("../app");

// describe("Testing Express Application Endpoints", () => {
//   it("GET /set-cookies - should return confirmation message", async () => {
//     const response = await request(app).get("/set-cookies");
//     expect(response.status).toBe(200);
//     expect(response.text).toBe("you got this cookie");
//   });

//   it("GET /read-cookies - should return empty cookies object", async () => {
//     const response = await request(app).get("/read-cookies");
//     expect(response.status).toBe(200);
//     expect(response.body).toEqual({});
//   });
// });
